Adds the abilty to use Cassandra as a Cache for TileCache ( http://tilecache.org/ ). 
Requires PyCassa ( http://github.com/pycassa/pycassa )

Todos:
- Add tests
- Make sure it really works.
- Add some examples (Using OpenLayers)

